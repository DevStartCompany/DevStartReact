import { createStitches } from "@stitches/react";

export const {
  styled,
  keyframes,
  config,
  createTheme,
  css,
  getCssText,
  globalCss,
  prefix,
  reset,
  theme,
} = createStitches({
  theme: {
    colors: {
      background: "#111922",
    },
  },
});
